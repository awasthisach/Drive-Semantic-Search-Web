# Optional local overrides (not required for production GitHub Pages deploy).
# OAuth uses Firebase + GIS client config (see firebase-applet-config.json).

# Public URL of the authenticated /embed worker (no API keys here).
VITE_EMBED_ENDPOINT=https://drive-semantic-embed.awasthi-sach.workers.dev
